# -*- coding: utf-8 -*-
"""使用教程 —— 独立的说明页面（Streamlit multipage 结构）。

本页面与「异常检测」主页完全独立：左侧导航切换即可打开，
不占用主页的任何空间，关闭/返回不影响主页的检测状态。

页面结构：先「系统说明」（通俗介绍系统是什么 / 能做什么 / 怎么实现），
再接具体操作教程（快速上手 + 数据源 / 参数 / 结果 / FAQ 详解）。
教程部分默认全部展开，参数逐项详细讲解。
"""
import sys
from pathlib import Path
import streamlit as st

PROJECT_ROOT = str(Path(__file__).resolve().parent.parent.parent)
st.set_page_config(page_title="使用教程", page_icon="📚", layout="wide")

sys.path.insert(0, PROJECT_ROOT)
sys.path.insert(0, PROJECT_ROOT + "/app")
import streamlit_app  # noqa: E402  复用主应用的样式与工具

streamlit_app.inject_css()


def render_tutorial():
    """使用教程页：系统说明（先） + 快速上手 + 参数速查 + 结果解读 + FAQ。默认全部展开，参数详细。"""
    st.markdown("""
    <div class="section-title"><span class="icon">📚</span> 使用教程</div>
    <div class="section-desc">本页先通俗介绍系统是什么、能做什么、怎么实现的，再给出四步上手教程与参数 / 结果 / FAQ 详解。下方内容默认全部展开，可随时收起。</div>
    """, unsafe_allow_html=True)

    # ═══════════════════════════════════════════════════════════
    #  系统说明（通俗介绍 → 功能作用 → 实现原理）
    # ═══════════════════════════════════════════════════════════
    st.markdown("""
    <div class="section-title"><span class="icon">💡</span> 一、系统说明</div>
    <div class="section-desc">先花三分钟了解这个系统：它是什么、能帮你做什么、背后是怎么工作的。</div>
    """, unsafe_allow_html=True)

    # 1. 这是什么
    st.markdown("#### 🌟 这是什么")
    st.markdown("""
    本系统是一个**面向时间序列数据的异常检测可视化平台**。

    **时间序列（time series）** 就是按时间先后顺序排列的一串数值。生活中到处都是：服务器的 CPU 占用率曲线、工厂传感器的温度读数、医院的心电图（ECG）波形、网站的访问流量、股票的每分钟价格……它们通常是连续的、自带时间顺序的「一条线」。

    这条线里往往会混入**异常**——突然冲高的尖峰、骤然掉底的凹陷、不该出现的趋势突变、原本规律的周期被打断，等等。这些异常背后往往意味着：机器故障、网络攻击、设备老化、或者身体出了状况，需要尽早发现。

    传统做法要么靠人盯着曲线看（累、还容易漏），要么写一堆 if 规则（换个场景就失效）。本系统用**深度学习（扩散模型）**自动干这件事：你把一段时序丢进去，它自动告诉你「哪一段是异常的、异常有多严重」，并把异常位置标出来。
    """)

    # 2. 能做什么（功能卡片）
    st.markdown("#### 🎯 系统能做什么（作用）")
    _func_cards = """
    <div class="metric-row">
      <div class="metric-card">
        <div style="font-size:0.95rem;font-weight:700;color:#1e293b;margin-bottom:4px;">🔍 自动异常检测</div>
        <div class="metric-desc">输入一段时序，自动输出每个时间窗口的「异常分数」，标出异常位置与严重程度</div>
      </div>
      <div class="metric-card tp">
        <div style="font-size:0.95rem;font-weight:700;color:#1e293b;margin-bottom:4px;">🆚 多算法对比</div>
        <div class="metric-desc">同一段数据上跑 Diffusion / AE / VAE / AnoGAN / 孤立森林，分数曲线叠加横向比较</div>
      </div>
      <div class="metric-card prec">
        <div style="font-size:0.95rem;font-weight:700;color:#1e293b;margin-bottom:4px;">📉 少样本检测</div>
        <div class="metric-desc">只喂极少量正常样本就能训练出可用的检测模型——本系统的核心创新与论文重点</div>
      </div>
      <div class="metric-card rec">
        <div style="font-size:0.95rem;font-weight:700;color:#1e293b;margin-bottom:4px;">📊 可视化解读</div>
        <div class="metric-desc">原始波形、异常分数曲线、分数分布图，异常区间与阈值一目了然</div>
      </div>
      <div class="metric-card f1">
        <div style="font-size:0.95rem;font-weight:700;color:#1e293b;margin-bottom:4px;">⬇️ 结果导出</div>
        <div class="metric-desc">一键下载 CSV（含分数 / 预测 / 真实标签），方便论文附录或二次分析</div>
      </div>
      <div class="metric-card tn">
        <div style="font-size:0.95rem;font-weight:700;color:#1e293b;margin-bottom:4px;">🧪 对比 / 消融</div>
        <div class="metric-desc">内置数据增强、条件扩散、多尺度三大优化的消融结果，验证每一项的价值</div>
      </div>
    </div>
    """
    st.markdown(_func_cards, unsafe_allow_html=True)

    # 3. 怎么实现
    st.markdown("#### ⚙️ 系统是怎么实现的（原理，通俗版）")
    st.markdown("""
    核心思想只有一句话：**用扩散模型的「重建误差」来判断异常**——模型越「认不出」某段数据，它就越可能是异常。用一个生活化的比喻：你让一个只看过「健康心电图」的医生去复查一张图，他能把「健康的部分」画得和原图几乎一样，但遇到「病态的部分」就画不准、会出现明显偏差——这些「画不准的地方」就是异常。

    - **训练阶段（只学「正常」）**：扩散模型（DDPM）在大量**正常**样本上做「加噪声 → 学去噪 → 还原原始波形」的训练。训练完成后，模型只「认识」正常数据的样子，对正常模式能高保真地重建出来。整个过程**完全不需要异常样本**，这正好契合「异常很难提前收集」的现实。
    - **检测阶段（看「重建得差不差」）**：把待检测的一段时序加噪，再让模型去噪重建。正常段模型很熟悉 → 重建得几乎一模一样 → 误差小 → 异常分数低；异常段模型没见过 → 重建得「面目全非」 → 误差大 → 异常分数高。最后用阈值（默认取 95 百分位）把高分窗口判为异常。
    - **一维 U-Net 去噪主干**：一个专门为时序设计的卷积网络，像「多层漏斗」一样逐层提取局部细节与全局趋势，再一层层还原，完成加噪 / 去噪。
    - **条件向量（均值 / 标准差 / 最大 / 最小）**：把整段窗口的 4 个统计特征作为「条件」一起喂给网络，让它根据数据自身分布自适应地重建，提升对不同量级、不同分布的鲁棒性（比如同样是异常，CPU 0~100 和温度 0~50 都能正确处理）。
    - **多尺度重建**：同时融合「粗粒度（整体趋势）」和「细粒度（局部抖动）」两层特征再重建，缓慢漂移、微小抖动这类细微异常也更容易被捕捉。
    - **少样本关键**：仅靠极少量正常样本 + 数据增强（人为造出更多正常样本变体）+ 条件扩散，模型就能学会「正常长什么样」，无需海量标注——这正是「少样本」场景的意义所在。

    > 💡 一句话总结：先让模型「只认识正常」，再用「它认不认得出来」当异常探测器。认得出 = 正常，认不出 = 可疑。
    """)

    st.markdown("---")
    st.caption("了解完系统，下面进入具体操作教程 👇")
    st.markdown("<div style='height:8px;'></div>", unsafe_allow_html=True)

    # ═══════════════════════════════════════════════════════════
    #  操作教程（快速上手 → 数据源 / 参数 / 结果 / FAQ）
    # ═══════════════════════════════════════════════════════════
    st.markdown("""
    <div class="section-title"><span class="icon">🚀</span> 二、操作教程</div>
    """, unsafe_allow_html=True)

    # 快速上手
    with st.container():
        st.markdown("### 🚀 快速上手（4 步）")
        steps = [
            ("① 选择数据", "左侧「数据源」选 **UCR 内置数据集**（默认 000，已带预训练权重、含真实标签）或切到「上传数据」拖入你的 CSV/TXT/XLSX。"),
            ("② 调参数", "按需设置窗口大小、检测算法、多尺度开关与检测档位。第一次用直接保持默认、档位选「快速」即可。"),
            ("③ 启动", "点击左侧「🚀 启动检测」，进度条跑完即出结果（默认档位约几十秒）。"),
            ("④ 看结果 / 导出", "查看指标卡、时序与分数图、异常分布；点「下载检测结果为 CSV」留存，或用「多模型对比」叠加其他算法横向比较。"),
        ]
        for title, desc in steps:
            st.markdown(f"- **{title}**：{desc}")

    st.markdown("<div style='height:14px;'></div>", unsafe_allow_html=True)

    # 数据源说明（展开）
    with st.expander("📁 数据源与上传格式", expanded=True):
        st.markdown("""
        **1）UCR 内置数据集（开箱即用，推荐新手）**
        - 来自公开基准 **UCR Anomaly Archive** 的 SYNTHETIC 子集，本系统已内置 8 个子集，默认选 `000`。
        - 每个子集都**自带真实异常标签**（0=正常，1=异常），所以检测完能直接算出 Precision / Recall / F1 / AUROC 这些客观指标。
        - 无需任何准备，选好即可检测——它用的就是论文训练好的权重。

        **2）上传你自己的数据**
        - **CSV / XLSX**：每行一个时间点（即一列数值）。如果文件里还有一列 0/1 的标签，上传后在左侧勾选「包含异常标签列」并指定是哪一列，系统就会自动算指标；不勾则只给异常分数、不评指标。
          - 示例：`value,label` 两列，value 是数值、label 是 0 或 1。
        - **TXT**：每行一个数值（纯单列，没有表头）。
        - 上传的数据会**直接用 UCR 预训练模型检测**，结果作演示参考。注意：上传数据分布和训练数据可能不同，分数绝对值仅供参考，重点看相对高低与异常位置。
        """)

    # 参数详细说明（展开，逐项大白话）
    with st.expander("🎛 参数详细说明（逐项讲解）", expanded=True):
        st.markdown("""
        下面把左侧每个参数都讲清楚：**它是什么、怎么影响结果、该填多少**。

        ---
        **① 窗口大小（Window Size）**
        - **是什么**：长时序会被切成一段段等长的小段（窗口），模型是「逐窗口」判断异常的。窗口大小就是每一段的长度（时间步数）。
        - **怎么选**：太小 → 看不全一个完整异常的形态，容易漏报；太大 → 一个窗口里正常和异常混在一起，定位会变模糊。常用 **50–300**，论文多在 ~100–200。
        - **举例**：如果你的数据是每秒 1 个点、一次异常大约持续 1 分钟，窗口设 **60~120** 比较合适，能完整包住异常又不至于太大。

        ---
        **② 少样本比例（Few-shot Ratio）**
        - **是什么**：训练时只用百分之多少的「正常」样本来训练模型。`1.0` = 用全部正常样本；`0.1` = 只用 10%。
        - **为什么重要**：真实场景往往拿不到大量标注数据，模型在「样本极少」时还能不能work，就是「少样本」能力的意义，也是本系统论文的重点。
        - **举例**：设 `0.1` 表示只用 10% 的正常样本训练，模型依然要能检测异常——比例越低越难、越能体现方法的优势。日常体验直接保持默认即可。

        ---
        **③ 检测算法（Algorithm）**
        - **Diffusion（默认，论文主推）**：就是本系统的扩散模型，用「重建误差」判异常，少样本表现最好。
        - **AE（自编码器）**：把数据压缩再还原，简单、快，但还原能力弱，异常分数区分度一般。
        - **VAE（变分自编码器）**：AE 的概率版，除了分数还能给出不确定性。
        - **AnoGAN**：用 GAN 在潜空间里搜索最匹配的向量来重建，需要梯度搜索、相对较慢。
        - **Isolation Forest（孤立森林）**：经典非深度方法，思想是「异常点更容易被随机切分孤立出来」；**不需要训练神经网络**，适合当快速基线对照。
        - **建议**：首发用 Diffusion；想看传统方法差距就叠加 Isolation Forest / AE 做对比。

        ---
        **④ 多尺度重建（Multi-scale）**
        - **是什么**：让模型同时看「粗粒度（整体趋势）」和「细粒度（局部抖动）」两层特征再重建。
        - **什么时候开**：异常很微弱、很缓慢（比如温度慢慢漂移几天才暴露）时建议打开；普通尖峰类异常默认关也够用。
        - **代价**：多算一层特征，计算量略增、速度略慢，但能抓到更隐蔽的异常。

        ---
        **⑤ 检测档位（Speed / Quality）**
        - **原理**：扩散模型去噪要「一步步迭代」把噪声去掉，步数越多重建越精细。档位本质就是控制去噪步数。
        - **标准**：步数最多，质量最高、最慢——写论文正式结果时用。
        - **快速（推荐）**：步数适中，质量与速度平衡——日常体验首选。
        - **极速**：步数最少，秒级出结果，精度略降（论文实测 AUROC 仅降约 1%，但快近一倍）。
        - **怎么选**：先「快速」看效果；要正式数字再切「标准」；只想秒看个大概用「极速」。

        ---
        **⑥ 检测窗口数（#Windows）**
        - **是什么**：这一次检测一共覆盖多少个窗口，也就是「看多长一段数据」。
        - **怎么选**：**20–500**，越大覆盖越全、越慢。想快速看效果先设 **50~100**；要扫完整段数据再调大。
        """)

    # 结果解读（展开）
    with st.expander("🔍 如何解读结果", expanded=True):
        st.markdown("""
        **1）指标卡（有标签时）**
        - `TP`（真正例）：异常被正确标出；`FP`（假正例）：正常被误报；`FN`（假负例）：异常被漏掉；`TN`（真负例）：正常被正确放过。
        - `Precision（精确率）= TP / (TP+FP)`：报出来的异常里有多少是真的。
        - `Recall（召回率）= TP / (TP+FN)`：真实异常里有多少被找出来了。
        - `F1 = 2·P·R / (P+R)`：P 和 R 的综合，越高越好。
        - `AUROC`：随机抽一个异常和一个正常，模型给异常更高分的概率；**越接近 1 越好**，0.5 等于瞎猜。
        - 无标签时：只给「检出数 / 阈值 / 总窗口」，指标仅供参考。

        **2）时序波形与异常分数图**
        - 上行：原始时序曲线。若有真实标签，异常区间用**红色阴影**标出。
        - 下行：异常分数曲线，**橙色虚线**是判定阈值，分数超过虚线的窗口被判为异常。

        **3）异常分数分布图**
        - 正常 / 异常分数的叠加直方图，直观看「阈值能不能把两类分开」——分得很开说明检测效果好。

        **4）导出 CSV**
        - 含「窗口索引 / 异常分数 / 预测 label / 真实 label」，可直接用 Excel 打开，方便论文附录或二次分析。

        **5）多模型对比**
        - 同一张图上叠加其他算法的分数曲线，便于横向比较「各家算法在哪些异常峰值上表现更好」。
        """)

    # FAQ（展开）
    with st.expander("❓ 常见问题 FAQ", expanded=True):
        st.markdown("""
        - **提示「未找到模型权重」？** 说明 `experiments/results/` 下缺权重文件。先运行 `python scripts/run_experiment.py` 生成权重，再启动检测。也可以在「关于版本」页看权重自检，确认 5 个文件都是 ✅。
        - **上传后提示没有数值列？** 确保你的文件以数值为主：CSV/XLSX 的时序列应是数字；TXT 每行一个数字。表头文字不会被判成数值列。
        - **极速档精度略低正常吗？** 正常，是去噪步数减少导致的预期现象（论文实测仅降约 1%），追求精度请选「标准」或「快速」。
        - **没有标签时怎么判定异常？** 采用 **95 百分位阈值**自动判定（分数排前 5% 的窗口算异常），此时指标只作参考、不评 P/R/F1。
        - **检测很慢怎么办？** 减小「检测窗口数」、档位选「极速」、或关闭「多尺度」。多模型对比会加载多个模型，首次较慢属正常（同进程内有缓存，二次切换很快）。
        - **想看完整离线实验？** 在「异常检测」主页底部已内置「对比实验 / 消融实验」结果表，对比了 Diffusion 与各基线、以及三大优化的贡献。
        - **我该先试哪个数据集？** 新手直接选默认 `000`（UCR 内置、带标签），保持默认参数、档位「快速」，点启动即可看到完整结果。
        """)


render_tutorial()
